[project]
name = "infer-json"
version = "0.2.0"
description = "Infer TypeScript/Go types from JSON/JSONL files"
requires-python = ">=3.12"

[dependency-groups]
dev = ["pytest>=8"]

[tool.black]
line-length = 120

[tool.isort]
profile = "black"
